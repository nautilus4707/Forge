# Quickstart

1. Install: `pip install -e .` (from project root)
2. Run: `python hello_agent.py` or `forge run -f hello_agent.yaml "Hello!"`
3. Customize: Edit hello_agent.yaml to change model, prompt, or tools
